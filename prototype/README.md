# Prototype

The original standalone-HTML prototype trunkline grew out of. Kept for reference.
The shippable app lives in [`../react-app/`](../react-app/).

- `Git-styled Finance Tracker.html` — entry HTML (loads the four `.jsx` files via Babel-standalone).
- `Git-styled Finance Tracker.bundle-src.html` — single-file bundled source.
- `Git-styled Finance Tracker (standalone).html` — single-file standalone build (~1.6 MB).
- `app.jsx`, `graph.jsx`, `data.jsx`, `tweaks-panel.jsx`, `styles.css` — the original modules.
